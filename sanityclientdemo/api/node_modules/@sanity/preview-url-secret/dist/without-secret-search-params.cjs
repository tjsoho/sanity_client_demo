'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var constants = require('./constants.cjs');

function withoutSecretSearchParams(url) {
  const newUrl = new URL(url);
  const { searchParams } = newUrl;
  searchParams.delete(constants.urlSearchParamPreviewPathname);
  searchParams.delete(constants.urlSearchParamPreviewSecret);
  searchParams.delete(constants.urlSearchParamPreviewPerspective);
  return newUrl;
}
function hasSecretSearchParams(url) {
  return url.searchParams.has(constants.urlSearchParamPreviewSecret);
}
function setSecretSearchParams(url, secret, redirectTo, perspective) {
  const newUrl = new URL(url);
  const { searchParams } = newUrl;
  searchParams.set(constants.urlSearchParamPreviewSecret, secret);
  searchParams.set(constants.urlSearchParamPreviewPathname, redirectTo);
  searchParams.set(constants.urlSearchParamPreviewPerspective, perspective);
  return newUrl;
}

exports.hasSecretSearchParams = hasSecretSearchParams;
exports.setSecretSearchParams = setSecretSearchParams;
exports.withoutSecretSearchParams = withoutSecretSearchParams;
//# sourceMappingURL=without-secret-search-params.cjs.map
